# .env is better suited for public variables, ie, variables that should not commited
# For secret variables is better to use DevServerControl tool with set_env_variable: ["KEY", "SECRET"]

# https://www.builder.io/c/docs/using-your-api-key
VITE_PUBLIC_BUILDER_KEY=__BUILDER_PUBLIC_KEY__
PING_MESSAGE="ping pong"

# Password Protection Configuration
# For Netlify deployment: Set the VITE_APP_PASSWORD environment variable in Netlify dashboard
# Site Settings → Build & Deploy → Environment → Edit variables
# Add: VITE_APP_PASSWORD = your-secure-password
#
# For local development/Builder.io:
# The password below will be shown on login page in dev mode
VITE_APP_PASSWORD=demo123
